const mostrarreceta = (receta) => {
    console. log(receta);
    switch (receta) {
        case "receta1": 
        console.log("mostrar1");
        break;
        case "receta2": 
        console.log("mostrar2");
        default:
            break;
    }
}